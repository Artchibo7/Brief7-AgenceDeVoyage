{% extends 'base.html.twig' %}

{% block title %}Editer Destination{% endblock %}

{% block body %}
    <h1>Editer destination</h1>

    {{ include('destination/_form.html.twig', {'button_label': 'Appliquer'}) }}

    <a class="btn btn-info" href="{{ path('app_destination_index') }}">retour à la liste</a>

    {{ include('destination/_delete_form.html.twig') }}
{% endblock %}
